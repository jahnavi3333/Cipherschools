#include <bits/stdc++.h> using namespace std;

// pass by value/pass by reference void mergeSortedArrays(vector<int> &a, int m, vector<int> &b, int n) {

int im-1, j-n-1, k

while (&&10) (

if (a[1] b[j]) { a[k]=a[i];

1--3 else {

a[k]. b[j];

k--;

while (j > 0) {

a[k] b[j];

k--;

int main() {

1;